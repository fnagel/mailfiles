plugin.tx_mailfiles_pi1 {
	templateFile = EXT:mailfiles/res/template.html

	mail {
		# email data
		to = mail@test.dev
		from = no-reply@domain.dev
		from_name =

		subject = New files uploaded

		# added to the email
		beforeMessage = TEXT
		beforeMessage.value = New files uploaded! This message was attached:<br /><br />

		afterMessage = TEXT
		afterMessage.value = <br /><br /><strong>Uploaded files:</strong><br /><br />
	}

	# set this if used via TS
	uid =
	configUid =

	# info message
	success = TEXT
	success.value = <p>Thanks, your files were submitted!</p><br />

	# error message
	no_files = TEXT
	no_files.value = <p>Please upload files before sending the form.</p><br />

	# form configuration
	form = COA
	form {
		wrap = <div>|</div>

		10 = FORM
		10 {
			enctype = application/x-www-form-urlencoded
			method = post
			prefix = mailfiles

			layout < tt_content.mailform.20.layout
			# When using newer EXT:form TypoScript:
			# layout < tt_content.mailform.20.form.layout

			10 = TEXTAREA
			10 {
				cols = 40
				name = message
				rows = 5
				label {
					value = Message
				}
			}
			20 = CHECKBOX
			20 {
				name = auto
				value = 1
				label {
					value = Auto-Send
				}
			}
			30 = SUBMIT
			30 {
				name = submit
				value = Submit
			}
		}
	}
}